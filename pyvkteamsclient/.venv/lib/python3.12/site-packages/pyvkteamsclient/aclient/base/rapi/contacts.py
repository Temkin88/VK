from typing import List, Union

from ...base.client import BaseClient
from ....client.decorators import api_version_check


class ContantsMethods(BaseClient):
    """
    Методы группы 'Контакты' из https://im-docs.corp.mail.ru/
    """

    async def rapi_getUserInfo(self, sn: str):
        """
        Возвращает всю информацию из анкеты пользователя. Кроме того:

        commonChats - количество общих чатов с целевой учетной записью
        (только для запроса информации о другой учетной записи)
        mute - учетная запись от лица которой происходит запрос заблокирована
        у целевой учетной записи
        (только для запроса информации о другой учетной записи)
        hasPassword - наличие пароля у учетной записи
        (только для запроса информации о себе)
        :param sn: UIN учетной записи
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/getUserInfo",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"sn": sn},
            },
        )

    async def rapi_getIdInfo(self, _id: str):
        """
        Метод позволяет определить тип сущности по id и возвращает её свойства.
        На вход можно передавать sn, nick, stamp пользователя или чата.
        Если ничего не найдено, то вернется 20000 с пустым массивом.
        :param _id: ID сущности
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/getIdInfo",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"id": _id},
            },
        )

    async def rapi_getIdInfoBatch(self, ids: List[str]):
        """
        Метод позволяет определить тип сущностей по их id
        и возвращает их свойства.
        На вход можно передавать sn, nick, stamp пользователей или чата.
        В ответ приходит объект,
        в котором расположены отношения id -> сущность.
        Если для id ничего не найдено, то в ответе он будет отсутствовать.

        Максимальное количество id в запросе - 100.
        :param ids: список ID сущностей
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/getIdInfoBatch",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"ids": ids},
            },
        )

    async def rapi_contacts_get(self, nodeleted: bool = False, nobot: bool = False):
        return await self.post(
            "rapi/contacts/get",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"nodeleted": nodeleted, "nobot": nobot},
            },
        )

    async def rapi_contacts_edit(self, sn: str, friendly: str):
        """
        Редактировать контакт из контакт листа
        На вход методу передается идентификатор пользователя (sn) и полное имя пользователя (friendly).
        :param sn: Уникальный идентификатор пользователя внутри сервиса
        :param friendly: Полное имя пользователя
        """
        return await self.post(
            "rapi/contacts/edit",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"sn": sn, "friendly": friendly},
            },
        )

    @api_version_check(102)
    async def rapi_contacts_mute(self, sn: str, duration: int = 3600, tieCounter: bool = True):
        if self.api_ver >= 106:
            params = {"sn": sn, "duration": duration, "tieCounter": tieCounter}
        else:
            params = {"sn": sn, "duration": duration}
        return await self.post(
            "rapi/contacts/mute",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": params,
            },
        )

    async def rapi_contacts_delete(self, targets: List[str]) -> dict:
        """
        В массиве targets перечислены скриннеймы пользователей или чатов,
        которые нужно удалить.
        Указанные контакты и чаты исчезнут из списка недавних чатов и из
        контакт листа.
        :param targets: список ID контактов
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/contacts/delete",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"targets": targets},
            },
        )

    async def rapi_getUserLastSeen(self, ids: list[Union[str, int]]) -> dict:
        """
        Получение lastseen для пользователей
        :param ids: список ID пользователей
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/getUserLastseen",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"ids": ids},
            },
        )

    @api_version_check(min_version=107)
    async def rapi_getUserPbxInfo(self, sn: str) -> dict:
        """
        Получить информацию о PBX-биндингах пользователя (телефонные номера)
        Возвращает список PBX-биндингов пользователя. Сейчас это его телефонные номера, в будущем - типы номеров и т.д.
        Если список пустой, то биндингов нет.
        :param sn: Уникальный идентификатор пользователя внутри сервиса
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/getUserPbxInfo",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"sn": sn},
            },
        )

    @api_version_check(min_version=105)
    async def rapi_contacts_counter(self, sn: str, enable: bool = False) -> dict:
        """
        Управление каунтером чата
        Позволяет пользователю учитывать/не учитывать количество непрочитанных сообщений выбранного контакта или
        группы в общем количестве непрочитанных
        :param sn: Уникальный идентификатор пользователя внутри сервиса
        :param enable: false - счётчик чата не учитывается в общем количестве непрочитанных
        true - счётчик чата учитывается в общем количестве непрочитанных
        :return: тело ответа сервера
        """
        return await self.post(
            "rapi/contacts/counter",
            json={
                "reqId": self.getReqId(),
                "aimsid": self.aimsid,
                "params": {"sn": sn, "enable": enable},
            },
        )
