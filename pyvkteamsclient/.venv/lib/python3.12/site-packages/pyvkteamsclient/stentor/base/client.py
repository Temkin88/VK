"""
!
@defgroup MessengerLib
@brief Класс для работы с аккаунтами
"""

import logging
import time
from datetime import datetime
from typing import Optional, Union, Literal
from urllib.parse import unquote, urlparse

import allure
import requests
from requests import Response, Session

from pyvkteamsclient.stentor.exceptions import (
    RatelimitException,
    MissingParameterException,
    ServerException,
    AccessDeniedException,
    RequestException,
    HttpNotFoundException,
)
from pyvkteamsclient.tracer import tracer


class BaseStentorClient(object):
    request_counter: int = 0
    logger = logging.getLogger(__name__)

    def __init__(
        self,
        session: Optional[Session] = None,
        api_url: str = "https://stentor-vkorobov.v3.im-sandbox.devmail.ru",
        env: Literal["SANDBOX", "ICQ", "SAAS", "PRE_SAAS", "VKTI", "PRE_VKTI", "TARM", "PRE_TARM"] = "SANDBOX",
    ):
        if session is None:
            session = Session()
        self.session = session
        self.session.verify = False

        self.env = env

        self.base_url = api_url

    @tracer.start_as_current_span("stentor.allure_attach")
    def allure_attach(self, response: requests.Response, return_str: bool = False):
        """
        Функция для логирования запроса-ответа в Allure отчете

        """
        date_str = str(datetime.now())
        headers_str = "\n".join([f"{k}: {v}" for k, v in response.request.headers.items()])

        text = f"{date_str}\n" f"{response.request.method} {response.request.url}\n" f"{headers_str}\n"

        if response.request.body:
            if isinstance(response.request.body, str):
                text += "\n" f"{unquote(response.request.body)}"

            elif isinstance(response.request.body, (bytes, bytearray)) and len(response.request.body) < 1000:
                try:
                    text += "\n" f"{unquote(response.request.body.decode())}"
                except UnicodeDecodeError:
                    text += "\n" f"Bytes {len(response.request.body)}"

            else:
                text += "\n" f"{type(response.request.body)}"

        text = text.strip()

        headers_str = "\n".join([f"{k}: {v}" for k, v in response.headers.items()])

        text += f"\n\n{response.status_code}\n" f"{headers_str}\n"

        if response.text:
            if isinstance(response.text, str):
                text += "\n" f"{response.text}"
            else:
                text += "\n" f"{response.content.decode()}"

        self.logger.info(text)

        url_path = urlparse(response.url).path.replace("/", "_")

        allure.attach(
            text,
            name=f"{url_path}.log.txt",
            extension=allure.attachment_type.TEXT,
        )

        if return_str:
            return text
        return None

    @tracer.start_as_current_span("stentor.restart_session")
    def restart_session(self): ...

    @tracer.start_as_current_span("stentor.get_url_name")
    def get_url_name(self, url: str) -> str:
        parsed_url = urlparse(url)

        return parsed_url.path

    @tracer.start_as_current_span("stentor.request")
    def request(
        self,
        method: Literal["GET", "POST", "PUT", "PATCH", "DELETE"],
        url: str,
        headers: Optional[dict] = None,
        params: Optional[dict] = None,
        json: Optional[dict] = None,
        data: Optional[dict] = None,
        files=None,
        ignore_check: bool = False,
    ) -> dict:
        """
        Прокси-метод для всех запросов
        """
        error = None

        for i in range(3):
            try:
                with self.session.request(
                    method=method,
                    url=url,
                    headers=headers,
                    params=params,
                    json=json,
                    data=data,
                    files=files,
                ) as response:
                    response.encoding = "utf-8"
                    self.allure_attach(response)
                    if not ignore_check:
                        response = self.check_response(response=response)
                        return response
                    else:
                        return response.json()
            except (
                RatelimitException,
                RequestException,
                MissingParameterException,
                ServerException,
                AccessDeniedException,
            ) as exc:
                error = exc
                self.logger.error(error)
                time.sleep(i)

        else:
            raise error

    @tracer.start_as_current_span("stentor.check_response")
    def check_response(self, response: Response) -> Union[dict, Exception]:
        """
        Проверка кода ответа API
        """
        if response.status_code == 429:
            raise RatelimitException(f"{response.request.path_url}")

        try:
            response_json = response.json()

            response_status_code = int(response_json.get("status", {}).get("code") or response.status_code)
            response_status_text = str(
                response_json.get("reason") or response_json.get("status", {}).get("reason") or "NoReason",
            )
        except Exception as error:
            self.logger.error(error)
            response_json = response
            response_status_code = response.status_code
            response_status_text = "NoStatusText"

        if 200 <= response_status_code < 400 or 20000 <= response_status_code < 40000:
            return response_json

        elif response_status_code == 403:
            raise AccessDeniedException(
                env=self.env,
                path_url=response.request.path_url,
                response_status_code=response_status_code,
                response_status_text=response_status_text,
                http_request_plain_text=self.allure_attach(response=response, return_str=True),
            )

        elif response_status_code == 404:
            raise HttpNotFoundException(
                env=self.env,
                path_url=response.request.path_url,
                response_status_code=response_status_code,
                response_status_text=response_status_text,
                http_request_plain_text=self.allure_attach(response=response, return_str=True),
            )

        elif response_status_code in [429, 430]:
            raise RatelimitException(
                env=self.env,
                path_url=response.request.path_url,
                response_status_code=response_status_code,
                response_status_text=response_status_text,
                http_request_plain_text=self.allure_attach(response=response, return_str=True),
            )

        elif response_status_code in (460, 462):
            raise MissingParameterException(
                env=self.env,
                path_url=response.request.path_url,
                response_status_code=response_status_code,
                response_status_text=response_status_text,
                http_request_plain_text=self.allure_attach(response=response, return_str=True),
            )

        elif 400 <= response_status_code < 500 or 40000 <= response_status_code < 50000:
            raise RequestException(
                env=self.env,
                path_url=response.request.path_url,
                response_status_code=response_status_code,
                response_status_text=response_status_text,
                http_request_plain_text=self.allure_attach(response=response, return_str=True),
            )

        else:
            raise ServerException(
                env=self.env,
                path_url=response.request.path_url,
                response_status_code=response_status_code,
                response_status_text=response_status_text,
                http_request_plain_text=self.allure_attach(response=response, return_str=True),
            )

    @tracer.start_as_current_span("stentor.get")
    @allure.step("[GET] Запрос к myteam-admin API {path}")
    def get(self, path: str, **kwargs) -> dict:
        """
        Оболочка для GET запросов к api ICQ/Myteam
        :param path: path для запроса
        :return: результат выполнения запроса (код и тело ответа)
        """
        return self.request(method="GET", url="/".join([self.base_url, path]), params=kwargs)

    @tracer.start_as_current_span("stentor.post")
    @allure.step("[POST] Запрос к myteam-admin API {path}")
    def post(
        self,
        path: str,
        params: Optional[dict] = None,
        json: Optional[Union[dict, list]] = None,
        data: Optional[dict] = None,
        files=None,
        headers=None,
    ) -> Union[dict, Response]:
        """
        Оболочка для POST запросов к api ICQ/Myteam
        :return: результат выполнения запроса (код и тело ответа)
        """
        return self.request(
            method="POST",
            url="/".join([self.base_url, path]),
            params=params,
            json=json,
            data=data,
            files=files,
            headers=headers,
        )

    @tracer.start_as_current_span("stentor.patch")
    @allure.step("[PATCH] Запрос к myteam-admin API {path}")
    def patch(
        self,
        path: str,
        params: Optional[dict] = None,
        json: Optional[dict] = None,
        data: Optional[dict] = None,
        files=None,
    ) -> Union[dict, Response]:
        """
        Оболочка для POST запросов к api ICQ/Myteam
        :return: результат выполнения запроса (код и тело ответа)
        """
        return self.request(
            method="PATCH",
            url="/".join([self.base_url, path]),
            params=params,
            json=json,
            data=data,
            files=files,
        )

    @tracer.start_as_current_span("stentor.put")
    @allure.step("[PUT] Запрос к myteam-admin API {path}")
    def put(
        self,
        path: str,
        params: Optional[dict] = None,
        json: Optional[dict] = None,
        data: Optional[dict] = None,
        files=None,
    ) -> Union[dict, Response]:
        """
        Оболочка для POST запросов к api ICQ/Myteam
        :return: результат выполнения запроса (код и тело ответа)
        """
        return self.request(
            method="PUT",
            url="/".join([self.base_url, path]),
            params=params,
            json=json,
            data=data,
            files=files,
        )

    @tracer.start_as_current_span("stentor.delete")
    @allure.step("[DELETE] Запрос к myteam-admin API {path}")
    def delete(
        self,
        path: str,
        params: Optional[dict] = None,
        json: Optional[dict] = None,
        data: Optional[dict] = None,
        files=None,
    ) -> Union[dict, Response]:
        """
        Оболочка для POST запросов к api ICQ/Myteam
        :return: результат выполнения запроса (код и тело ответа)
        """
        return self.request(
            method="DELETE",
            url="/".join([self.base_url, path]),
            params=params,
            json=json,
            data=data,
            files=files,
        )
